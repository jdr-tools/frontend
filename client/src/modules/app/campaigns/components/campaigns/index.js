import campaignsComponent from './campaigns_component.js'

const campaigns = angular.module('arkaan.frontend.campaigns.components.campaigns', [])
  .component('campaigns', campaignsComponent)
  .name

export default campaigns
