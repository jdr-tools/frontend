import campaignCardComponent from './campaign_card_component'

export default angular.module('arkaan.frontend.campaigns.components.campaign_card', [])
  .component('campaignCard', campaignCardComponent)
  .name