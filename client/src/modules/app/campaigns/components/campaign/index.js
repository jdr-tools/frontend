import campaignComponent from './campaign_component.js'

const campaign = angular.module('arkaan.frontend.campaigns.components.campaign', [])
  .component('campaign', campaignComponent)
  .name

export default campaign
