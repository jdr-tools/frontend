import confirmationModal from './confirmation_modal_component'

const confirmation = angular.module('arkaan.frontend.components.confirmation', [])
  .component('confirmationModal', confirmationModal)
  .name

export default confirmation
