import mainMenuComponent from './main_menu_component'

const mainMenu = angular.module('arkaan.frontend.components.main_menu', [])
  .component('mainMenu', mainMenuComponent)
  .name

export default mainMenu
